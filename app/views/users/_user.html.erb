<li>
  <%= gravatar_for user, size: Settings.length.name %>
  <%= link_to user.name, user %>
  <% if admin_user user %>
    <%= link_to t("app.views.users._user.delete"), user, method: :delete, data: {confirm: t("app.views.users._user.content")} %>
  <% end %>
</li>
